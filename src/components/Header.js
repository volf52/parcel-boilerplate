const Header = () => {
    const template = `
        <header>
            <h1>My parcel app</h1>
            <p>Boiler plate for Parcel</p>
        </header>
    `;

    return template;
};

export default Header;
